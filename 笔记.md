# docker配置
1. docker desktop用clash代理的tun可以直接下载docker hub的镜像。
2. 下载镜像后，可给dockerfile 添加国内镜像源，提升docker环境配置下载。
3. docker nginx修改docker-entrypoint.sh的行尾序列由CRLF-->LF![alt text](image.png)
4. launch.json需要放在.vscode下才能检测到debug